<?xml version="1.0" encoding="utf-8"?>
<!--
  Production traffic must use HTTPS. Cleartext is permitted only for the local
  development hosts (Android emulator loopback 10.0.2.2 and localhost) so the app
  can talk to a dev Django server, while the production domain is HTTPS-only.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
